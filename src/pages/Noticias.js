import React from "react";
import { Typography, Container } from "@mui/material";

const Noticias = () => (
  <Container>
    <Typography variant="h4" gutterBottom>
      Noticias
    </Typography>
    <Typography>Consulta nuestras últimas noticias y mantente informado.</Typography>
  </Container>
);

export default Noticias;
