import React from "react";
import { Typography, Container } from "@mui/material";

const Programacion = () => (
  <Container>
    <Typography variant="h4" gutterBottom>
      Programación
    </Typography>
    <Typography>Descubre nuestra programación diaria y no te pierdas nada.</Typography>
  </Container>
);

export default Programacion;
